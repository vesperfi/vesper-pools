name: vaAVAX with TraderJoe Leverage.
on:
  pull_request:
    paths:
      - "test/avalanche/vaavax/trader-joe-leverage.js"
      - "contracts/strategies/Strategy.sol"
      - "contracts/strategies/compound/CompoundLeverageStrategy.sol"
      - "contracts/strategies/compound/CompoundLeverageAvalancheCore.sol"
      - "contracts/strategies/compound/TraderJoeLeverageStrategy.sol"

jobs:
  vaavax-trader-joe-leverage-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.3.1
        with:
          submodules: true
      - run: cat "${GITHUB_WORKSPACE}/.github/avalanche.env.properties" >> $GITHUB_ENV

      - name: Use Node.js
        uses: actions/setup-node@v2
        with:
          node-version-file: ".nvmrc"

      - name: Run npm install
        run: npm install

      - name: Running tests for vaAVAX TraderJoe Leverage
        run: npm test "test/avalanche/vaavax/trader-joe-leverage.js"
