name: vaAPE with Rari Fuse
on:
  pull_request:
    paths:
      - "test/vaApe/rari-fuse.js"
      - "contracts/strategies/Strategy.sol"
      - "contracts/strategies/compound/CompoundStrategy.sol"
      - "contracts/strategies/rari-fuse/RariCore.sol"
      - "contracts/strategies/rari-fuse/RariFuseStrategy.sol"
      - "contracts/strategies/rari-fuse/RariFuseStrategyAPE.sol"

jobs:
  vaape-rarifuse-strategy-test:
    runs-on: ubuntu-latest
    env:
      NODE_URL: ${{ secrets.NODE_URL }}
    steps:
      - uses: actions/checkout@v2.3.1
        with:
          submodules: true
      - run: cat "${GITHUB_WORKSPACE}/.github/env.properties" >> $GITHUB_ENV

      - name: Use Node.js
        uses: actions/setup-node@v2
        with:
          node-version-file: '.nvmrc'

      - name: Run npm install with node v${{ matrix.node }}
        run: npm install

      - name: Running tests for vaAPE with Rari Fuse
        run: BLOCK_NUMBER=14454500 npm test "test/vaApe/rari-fuse.js"
